{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "git-submodules": {
    "enabled": true,
    "versioning": "git",
    "fileMatch": [
      "(^|/)\\.gitmodules$"
    ]
  },
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "(^|/)PKGBUILD$"
      ],
      "matchStrings": [
        "pkgver=(?<currentValue>.*) # renovate: datasource=(?<datasource>.*) depName=(?<depName>.*)"
      ],
      "extractVersionTemplate": "^v?(?<version>.*)$"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "(^|/)PKGBU1LD$"
      ],
      "matchStrings": [
        "pkgver=(?<currentValue>.*) # renovate: datasource=(?<datasource>.*) depName=(?<depName>.*)"
      ],
      "extractVersionTemplate": "^v?(?<version>.*)$"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "(^|/)PKGBUILD$"
      ],
      "matchStrings": [
        "pkgver=(?<currentValue>.*) # renovate"
      ],
      "datasourceTemplate": "custom.typora",
      "depNameTemplate": "typora",
      "versioningTemplate": "regex:^typora_(\\d+\\.\\d+\\.\\d+)_arm64.deb$"
    }
  ],
  "customDatasources": {
    "typora": {
      "defaultRegistryUrlTemplate": "https://www.typora.io/linux/Packages",
      "format": "plain"
    }
  },
  "packageRules": [
    {
      "description": "自动合并 submodule 更新",
      "matchManagers": [
        "git-submodules"
      ],
      "matchDatasources": [
        "git-refs"
      ],
      "enabled": true,
      "automerge": true,
      "ignoreTests": true,
      "automergeType": "branch"
    },
    {
      "description": "自动合并自定义 PKGBUILD 更新",
      "matchDatasources": [
        "github-tags",
        "custom.typora"
      ],
      "automerge": true,
      "ignoreTests": true,
      "automergeType": "branch"
    },
    {
      "matchDatasources": [
        "custom.typora"
      ],
      "extractVersion": "(?<version>.+)/$"
    }
  ]
}